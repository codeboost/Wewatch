#UNFINISHED
fs = require 'fs'
{exec} = require 'child_process'
stitch = require 'stitch'


task 'compile', 'Compile coffeescript files', ->
	exec 'iced -c -o js/ ../client'

task 'stitch', 'Stitch all source files to app.js', ->
